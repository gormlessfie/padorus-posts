<div>
  <% @posts.each do |post| %>
    <div class='box'>
      <%= post.title %><br>
      <%= post.body %>
      <%= link_to 'read more', user_post_path(post.user, post) %>
    </div>
  <% end %>
</div>
